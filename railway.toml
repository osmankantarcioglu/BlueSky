[build]
builder = "nixpacks"

[build.nixpacksPlan.phases.install]
cmds = ["pip install -r requirements-server.txt"]

[deploy]
startCommand = "waitress-serve --host=0.0.0.0 --port=$PORT feed_generator.server:app"
